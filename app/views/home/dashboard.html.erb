<div class="columns is-vcentered">
  <div class="column is-8">
    <div class="card">
        <div class="card-content">
            <div class="media">
            <div class="media-left">
                <% if @user.avatar.attached? %>
                    <figure class="image is-96x96">
                        <%= image_tag @user.avatar %>
                    </figure>
                <% else %>
                    <figure class="image is-48x48">
                <img src="https://bulma.io/images/placeholders/96x96.png" alt="imagen">
                </figure>
                <% end %>
            </div>
            <div class="media-content">
                <p class="title is-4">
                    <% if @user.first_name? %>
                        <%= @user.full_name %>
                    <% else %>
                        Complete su perfil
                    <% end %>
                </p>
                <p class="subtitle is-6"> <%= @user.email%> </p>
                <% if @user.birthday %>
                    <p class="subtitle is-6"> <strong>Birthday:</strong> <%= @user.birthday.strftime('%B %d') %> </p>
                <% end %>
            </div>
            </div>

            <div class="content">
                <table class="table is-bordered is-striped is-narrow is-hoverable is-small">
                    <thead>
                    </thead>
                    <tbody>
                        <tr>
                            <th>Age</th>
                            <% if @user.birthday? %>
                                <td><%= @user.age %> years</td>
                            <% else %>
                                <td>complete profile</td>
                            <%end%>
                        </tr>
                        <tr>
                            <th>Height</th>
                            <% if @user.height? %>
                                <td><%= @user.height %> m</td>
                            <% else %>
                                <td>complete profile</td>
                            <%end%>
                        </tr>
                         <tr>
                            <th>Weight</th>
                            <% if @user.weight? %>
                                <td><%= @user.weight %> kg</td>
                            <% else %>
                                <td>complete profile</td>
                            <%end%>
                        </tr>
                    </tbody>
                </table>
            Los datos de la grafica comprenden 30 dias de ingesta de calorias y de quema
            de esas mismas dia a dia.
            <br>
            <time> **Last Update for Profile: <%= @user.updated_at %> </time>
            </div>
        </div>
    </div>
  </div>
  <div class="column">
    <div class="card">
    <header class="card-header">
        <p class="card-header-title">
        IMC
        </p>
        <button class="card-header-icon" aria-label="more options">
        <span class="icon">
            <i class="fas fa-check" aria-hidden="true"></i>
        </span>
        </button>
    </header>
    <div class="card-content">
    <div class="content">
      Indice de Masa Corporal
      <p>Formula: <pre>Weight(kg) ÷ (Height (m))^2</pre> </p>
      <% if @user.height? && @user.weight? %>
        <p> <strong><%= @user.imc %></strong> </p>
      <% else %>
        <p> Complete Profile </p>
      <% end %>
    </div>
  </div>
    </div>
  </div>
</div>
<div class="columns">
<%= line_chart @data, download: true %>
</div>